name: Sync Backend to Hugging Face

on:
  push:
    branches: [main]
    paths:
      - 'backend/**' # Ne se déclenche que lorsque le back est modifié

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to HF
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # 1. Configurer l'identité Git
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          
          # 2. Extraire le dossier backend vers une branche temporaire
          git subtree split --prefix backend -b deploy-branch
          
          # 3. Pousser la branche locale "deploy-branch" vers la branche "main" distante
          git push -f https://NS78:$HF_TOKEN@huggingface.co/spaces/NS78/heart-disease-api deploy-branch:main
